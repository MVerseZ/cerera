# План интеграции Pallada VM в Cerera

## 📊 ОБЩАЯ КАРТИНА ПРОЕКТА

### Архитектура Cerera
- **Блокчейн**: PoW консенсус (Gigea), майнинг, валидация
- **Хранилище**: Vault (D5) - хранит StateAccount с балансами
- **Транзакции**: LegacyTxType, CoinbaseTxType, FaucetTxType, AppTxType, ApiTxtype
- **Валидация**: CoreValidator.ExecuteTransaction() - выполняет транзакции
- **Сеть**: IceNet (P2P), Mesh (DHT)

### Текущее состояние выполнения транзакций
- ✅ Простые переводы (LegacyTxType)
- ✅ Faucet транзакции
- ✅ Coinbase транзакции
- ⏳ **Выполнение контрактов/байткода** - **VM готова, интеграция в процессе**
- ✅ **Система газа для VM** - **РЕАЛИЗОВАНО**
- ✅ **Хранение кода контрактов** - **РЕАЛИЗОВАНО**

---

## ✅ СИЛЬНЫЕ МЕСТА

### 1. **Готовая инфраструктура**
- ✅ Система транзакций с полями `Data` и `Payload` (можно использовать для байткода)
- ✅ StateAccount имеет поле `CodeHash []byte` (можно хранить хеш кода)
- ✅ Система газа уже есть (Gas, GasPrice в транзакциях)
- ✅ Vault для хранения состояния аккаунтов
- ✅ Валидатор с методом ExecuteTransaction

### 2. **Архитектура**
- ✅ Модульная структура (validator, chain, storage)
- ✅ Service Registry для регистрации сервисов
- ✅ Типизированные транзакции (TxData interface)
- ✅ Поддержка разных типов транзакций

### 3. **Pallada VM**
- ✅ Полностью рабочая стековая VM
- ✅ 30+ опкодов реализовано
- ✅ Полное тестовое покрытие
- ✅ Оптимизированная производительность
- ✅ 256-битные слова (совместимо с big.Int)

---

## ⚠️ СЛАБЫЕ МЕСТА / ПРОБЛЕМЫ

### 1. **Отсутствие системы контрактов** (частично решено)
- ✅ Нет хранения байткода контрактов (только CodeHash) - **РЕШЕНО: добавлено StoreContractCode/GetContractCode**
- ⚠️ Нет различения между обычными аккаунтами и контрактами - **CodeHash указывает на наличие кода**
- ❌ Нет механизма создания контрактов (CREATE) - **Планируется в ЭТАП 5**
- ❌ Нет вызова контрактов (CALL) - **Планируется в ЭТАП 6**

### 2. **Интеграция газа** (решено)
- ✅ Газ есть в транзакциях, но не используется для VM - **РЕШЕНО: GasMeter реализован**
- ✅ Нет подсчета реального расхода газа при выполнении - **РЕШЕНО: UseGas для каждой операции**
- ✅ Нет системы лимитов газа для VM - **РЕШЕНО: GasLimit и проверка в UseGas**

### 3. **Хранилище контрактов** (решено)
- ✅ StateAccount.CodeHash хранит только хеш, не сам код - **РЕШЕНО: код хранится в pogreb**
- ✅ Нет отдельного хранилища для байткода контрактов - **РЕШЕНО: хранилище в Vault**
- ✅ Нет механизма загрузки кода контракта по адресу - **РЕШЕНО: GetContractCode**

### 4. **Контекст выполнения** (решено)
- ✅ VM не имеет доступа к:
  - ✅ Адресу вызывающего (Caller) - **РЕШЕНО: Context.Caller**
  - ✅ Адресу контракта (Address) - **РЕШЕНО: Context.Address**
  - ✅ Значению транзакции (Value) - **РЕШЕНО: Context.Value**
  - ✅ Данным транзакции (Input/Data) - **РЕШЕНО: Context.Input**
  - ⚠️ Блокчейн информации (block number, timestamp) - **РЕШЕНО: Context.BlockInfo (структура готова)**

### 5. **Типы транзакций** (в работе)
- ⚠️ AppTxType и ApiTxtype определены, но не используются - **Планируется в ЭТАП 3**
- ⚠️ Непонятно, какой тип использовать для контрактов - **Планируется в ЭТАП 3**

---

## 🎯 ПЛАН ИНТЕГРАЦИИ

### ✅ ЭТАП 1: Расширение VM для работы с контекстом блокчейна - ЗАВЕРШЕН

**Задачи:**
1. ✅ Добавить Context в VM (Caller, Address, Value, Input, BlockInfo)
2. ✅ Реализовать блокчейн-опкоды (ADDRESS, CALLER, CALLVALUE, CALLDATALOAD, CALLDATASIZE, CALLDATACOPY)
3. ✅ Добавить систему газа в VM (GasLimit, GasUsed)
4. ⏳ Интегрировать с Vault для чтения балансов (частично - доступ к Vault через Context)

**Файлы изменены:**
- ✅ `context.go` - создана структура Context
- ✅ `gas.go` - создана система расчета газа
- ✅ `vm.go` - добавлена поддержка Context и газа
- ✅ `opcodes.go` - добавлены новые опкоды
- ✅ `operations.go` - реализованы операции для новых опкодов
- ✅ `context_test.go` - созданы тесты (8 тестов)

**Результаты:**
- ✅ 42 теста VM проходят
- ✅ Все опкоды работают корректно
- ✅ Система газа функционирует

**Оценка:** Средняя сложность - ВЫПОЛНЕНО

---

### ✅ ЭТАП 2: Хранилище кода контрактов - ЗАВЕРШЕН

**Задачи:**
1. ✅ Добавить хранилище байткода контрактов в Vault
2. ✅ Методы: `StoreContractCode(address, code)`, `GetContractCode(address)`, `HasContractCode(address)`
3. ✅ Использовать CodeHash для быстрого поиска
4. ✅ Интеграция с существующим pogreb хранилищем

**Файлы изменены:**
- ✅ `storage/vault.go` - добавлены методы хранения кода
- ✅ `storage/vault_contract_test.go` - созданы тесты (6 тестов)
- ✅ Интерфейс `Vault` обновлен

**Результаты:**
- ✅ 6 тестов для хранения кода проходят
- ✅ Код сохраняется в pogreb с префиксом "code:"
- ✅ CodeHash автоматически вычисляется и сохраняется в StateAccount
- ✅ Все тесты storage проходят (28 тестов)

**Оценка:** Средняя сложность - ВЫПОЛНЕНО

---

### ЭТАП 3: Тип транзакции для контрактов

**Задачи:**
1. Определить тип транзакции для вызова контракта (использовать AppTxType?)
2. Определить тип для создания контракта (новый ContractCreateTxType?)
3. Обработка транзакций с `To == nil` как создание контракта
4. Обработка транзакций с `Data != nil` как вызов контракта

**Файлы для изменения:**
- `types/transaction.go` - возможно добавить новый тип
- `validator/validator.go` - обработка новых типов

**Оценка:** Низкая-средняя сложность

---

### ЭТАП 4: Интеграция в ExecuteTransaction

**Задачи:**
1. Определить, когда выполнять VM (если To имеет код или Data не пустой)
2. Загрузить код контракта из хранилища
3. Создать Context для VM
4. Выполнить VM с байткодом
5. Обработать результат (RETURN, REVERT)
6. Обновить состояние (балансы, storage)

**Файлы для изменения:**
- `validator/validator.go` - метод ExecuteTransaction
- Создать `validator/contract_executor.go` - логика выполнения контрактов

**Оценка:** Высокая сложность

---

### ЭТАП 5: Создание контрактов (CREATE)

**Задачи:**
1. Обработка транзакций с `To == nil` и `Data != nil`
2. Генерация адреса контракта (nonce-based или CREATE2)
3. Сохранение байткода в хранилище
4. Выполнение конструктора контракта
5. Сохранение результата (адрес контракта)

**Файлы для изменения:**
- `validator/contract_executor.go` - метод CreateContract
- `vm.go` - поддержка CREATE опкода

**Оценка:** Высокая сложность

---

### ЭТАП 6: Вызов контрактов (CALL)

**Задачи:**
1. Обработка транзакций с `To != nil` и кодом контракта
2. Загрузка кода контракта
3. Выполнение VM с входными данными (Data)
4. Обработка результата
5. Обновление состояния

**Файлы для изменения:**
- `validator/contract_executor.go` - метод CallContract
- `vm.go` - поддержка CALL опкодов

**Оценка:** Высокая сложность

---

### ЭТАП 7: Storage для контрактов

**Задачи:**
1. Добавить Storage в StateAccount (или отдельное хранилище)
2. Реализовать SLOAD/SSTORE опкоды
3. Интеграция с Vault или отдельным хранилищем
4. Merkle trie для storage (как в Ethereum)

**Файлы для изменения:**
- `types/account.go` - добавить Storage
- `operations.go` - реализовать SLOAD/SSTORE
- Возможно создать `storage/contract_storage.go`

**Оценка:** Средняя-высокая сложность

---

### ЭТАП 8: Тестирование и отладка

**Задачи:**
1. Тесты для создания контрактов
2. Тесты для вызова контрактов
3. Тесты для газа
4. Тесты для storage
5. Интеграционные тесты

**Оценка:** Средняя сложность

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Точки интеграции

1. **validator/validator.go:ExecuteTransaction()**
   - Проверка типа транзакции
   - Определение: контракт или обычный перевод
   - Вызов VM при необходимости

2. **storage/vault.go**
   - Методы для хранения/загрузки кода контрактов
   - Интеграция с существующим pogreb

3. **types/account.go**
   - Возможно расширение StateAccount
   - Флаг "это контракт" (Type field уже есть)

4. **vm.go**
   - Добавление Context
   - Интеграция с Vault для чтения состояния

### Реализованные изменения в VM

1. ✅ **Context структура:**
```go
type Context struct {
    Caller    types.Address
    Address   types.Address  // Адрес контракта
    Value     *big.Int
    Input     []byte         // tx.Data()
    GasLimit  uint64
    GasPrice  *big.Int
    BlockInfo *BlockInfo     // номер блока, timestamp и т.д.
}
```

2. ✅ **Реализованные опкоды:**
   - ✅ ADDRESS (0x90), CALLER (0x91), CALLVALUE (0x92)
   - ✅ CALLDATALOAD (0x93), CALLDATASIZE (0x94), CALLDATACOPY (0x95)
   - ⏳ SLOAD, SSTORE (storage) - планируется в ЭТАП 7
   - ⏳ CREATE, CALL, DELEGATECALL - планируется в ЭТАП 5-6
   - ⏳ BLOCKHASH, TIMESTAMP, NUMBER - планируется позже

3. ✅ **Система газа:**
   - ✅ Подсчет газа для каждой операции
   - ✅ Проверка лимита газа
   - ✅ Возврат использованного газа
   - ✅ GasMeter с методами UseGas, RefundGas, GasUsed

### Реализованные изменения в Vault

1. ✅ **Методы хранения кода:**
   - ✅ `StoreContractCode(address, code)` - сохраняет код и вычисляет CodeHash
   - ✅ `GetContractCode(address)` - получает код из pogreb
   - ✅ `HasContractCode(address)` - проверяет наличие кода

2. ✅ **Хранилище:**
   - ✅ Код хранится в pogreb с ключом "code:" + address.Bytes()
   - ✅ CodeHash автоматически сохраняется в StateAccount
   - ✅ Проверка целостности кода через сравнение хешей

---

## 📋 ПРИОРИТЕТЫ

### Высокий приоритет (MVP)
1. ✅ ЭТАП 1: Контекст и блокчейн-опкоды - **ЗАВЕРШЕН**
2. ✅ ЭТАП 2: Хранилище кода - **ЗАВЕРШЕН**
3. ⏳ ЭТАП 3: Тип транзакции - **В РАБОТЕ**
4. ⏳ ЭТАП 4: Интеграция в ExecuteTransaction - **СЛЕДУЮЩИЙ**

### Средний приоритет
5. ✅ ЭТАП 5: Создание контрактов
6. ✅ ЭТАП 7: Storage для контрактов

### Низкий приоритет
7. ✅ ЭТАП 6: Вызов контрактов (CALL между контрактами)
8. ✅ ЭТАП 8: Расширенное тестирование

---

## ⚡ РИСКИ И ПРОБЛЕМЫ

### Технические риски
1. **Производительность**: VM может быть медленной для больших контрактов
2. **Газ**: Нужна точная система расчета газа
3. **Хранилище**: Может потребоваться оптимизация для больших объемов
4. **Совместимость**: Изменения могут сломать существующие транзакции

### Архитектурные риски
1. **Обратная совместимость**: Существующие транзакции должны работать
2. **Масштабируемость**: Хранение кода всех контрактов
3. **Безопасность**: Валидация байткода, защита от DoS

---

## 🎯 РЕКОМЕНДАЦИИ

### Начать с:
1. **Минимальная интеграция**: Добавить Context в VM, реализовать базовые блокчейн-опкоды
2. **Простое хранилище**: Хранить код контрактов в памяти/Vault
3. **Один тип транзакции**: Использовать AppTxType для контрактов
4. **Базовое выполнение**: Выполнять VM при наличии Data в транзакции

### Избегать на первом этапе:
- Сложные CALL между контрактами
- CREATE2 с salt
- Полная система storage (можно начать с простого map)
- Оптимизации производительности (сначала сделать работающим)

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. ✅ **ЭТАП 1 завершен** - Context и блокчейн-опкоды реализованы
2. ✅ **ЭТАП 2 завершен** - Хранилище кода контрактов реализовано
3. ⏳ **ЭТАП 3** - Определить тип транзакции для контрактов (AppTxType)
4. ⏳ **ЭТАП 4** - Интегрировать VM в ExecuteTransaction
5. ⏳ **ЭТАП 5** - Реализовать создание контрактов (CREATE)
6. ⏳ **ЭТАП 6** - Реализовать вызов контрактов (CALL)

---

## 💡 УНИКАЛЬНЫЕ ВОЗМОЖНОСТИ

После интеграции Cerera получит:
- ✅ Собственная VM (не EVM) - **РЕАЛИЗОВАНО**
- ✅ 256-битные слова (больше чем EVM) - **РЕАЛИЗОВАНО**
- ✅ Интеграция с существующей системой газа - **РЕАЛИЗОВАНО**
- ✅ Гибкая архитектура для расширения - **РЕАЛИЗОВАНО**
- ✅ Хранилище кода контрактов - **РЕАЛИЗОВАНО**

## 📊 ТЕКУЩИЙ СТАТУС

### Выполнено:
- ✅ **ЭТАП 1**: Context, блокчейн-опкоды, система газа (100%)
- ✅ **ЭТАП 2**: Хранилище кода контрактов (100%)

### В процессе:
- ⏳ **ЭТАП 3**: Тип транзакции для контрактов (0%)

### Ожидает выполнения:
- ⏳ **ЭТАП 4**: Интеграция в ExecuteTransaction (0%)
- ⏳ **ЭТАП 5**: Создание контрактов (CREATE) (0%)
- ⏳ **ЭТАП 6**: Вызов контрактов (CALL) (0%)
- ⏳ **ЭТАП 7**: Storage для контрактов (0%)
- ⏳ **ЭТАП 8**: Расширенное тестирование (0%)

### Прогресс: 2/8 этапов (25%)
